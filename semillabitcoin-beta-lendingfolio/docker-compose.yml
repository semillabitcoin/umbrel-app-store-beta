version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: ${APP_SEMILLABITCOIN_BETA_LENDINGFOLIO_IP}
      APP_PORT: ${APP_SEMILLABITCOIN_BETA_LENDINGFOLIO_PORT}
      # Financial data → require the Umbrel login. Explicit so it never relies on a store default.
      PROXY_AUTH_ADD: "true"

  app:
    # Pin the multi-arch digest after the GHCR build (docker buildx imagetools inspect ...).
    image: ghcr.io/semillabitcoin/lendingfolio:v0.1.23-beta@sha256:4f46f161bdbab0189b0da106738559554eb3278754394946651aa13df34468a3
    init: true
    restart: on-failure
    stop_grace_period: 1m
    volumes:
      - ${APP_DATA_DIR}/data:/data
    environment:
      WEB_PORT: "4042"
      WEB_BIND: "0.0.0.0"
      DATA_DIR: "/data"
      APP_VERSION: "0.1.23-beta"
      # electrs (for the future electrs-local BTC provider). Injected by the 'electrs' dependency.
      ELECTRS_HOST: ${APP_ELECTRS_NODE_IP}
      ELECTRS_PORT: ${APP_ELECTRS_NODE_PORT}
      ELECTRS_TIMEOUT_MS: "30000"
      # Egress over Tor (no IP leak) for services without a local server; user can opt into clearnet.
      TOR_PROXY_IP: ${TOR_PROXY_IP}
      TOR_PROXY_PORT: ${TOR_PROXY_PORT}
      # Local Liquid esplora (4rkad-electrs-liquid :3000). Node-specific: that app doesn't export its IP.
      # If reachable, Liquid uses it directly (no Tor); otherwise it falls back to external over Tor.
      ELECTRS_LIQUID_HTTP: "http://10.21.0.35:3000"
      # Bitcoin Core RPC — HEALTH ONLY (node up / height). Not used for address history or broadcast.
      BITCOIN_RPC_HOST: ${APP_BITCOIN_NODE_IP}
      BITCOIN_RPC_PORT: ${APP_BITCOIN_RPC_PORT}
      BITCOIN_RPC_USER: ${APP_BITCOIN_RPC_USER}
      BITCOIN_RPC_PASS: ${APP_BITCOIN_RPC_PASS}
      # Optional local Mempool/Esplora REST (Umbrel Mempool app) → selectable BTC source. Node-specific
      # (that app's IP isn't injected without a dependency).
      MEMPOOL_LOCAL_URL: "http://10.21.21.26:3006"
      # Optional local price oracle (lunaticoin "El Oráculo") → selectable price source. Node-specific.
      ELORACULO_URL: "http://10.21.22.61:7777"
      APP_HIDDEN_SERVICE: ${APP_HIDDEN_SERVICE}
    networks:
      default:
        ipv4_address: ${APP_SEMILLABITCOIN_BETA_LENDINGFOLIO_IP}
